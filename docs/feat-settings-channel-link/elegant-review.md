# 設定機能のエレガントさ再検証（2026-09-24）

対象は今回の作業ツリーにある設定機能と共通画面部品。既存の評価・対策を判断の前提にせず、画面、実装、仕様、テストから改めて確認した。ファイルの削除を「思考リセット」とは扱わない。

## 30種の思考法と発見

| # | 思考法 | 新たに確認した事実と改善判断 |
|---|---|---|
| 1 | 批判的思考 | 「画像どおり6区画」は画像の5カードと一致しない。画像は視覚資料として扱い、追加のメンバー区画を明記する。 |
| 2 | 演繹思考 | `GET /api/settings` にメンバーは含まれない。6区画のUIを1応答で返すという記述を訂正する。 |
| 3 | 帰納的思考 | 同じ「6区画」が複数の文書に複写され、同じ誤解が広がった。現行構成の正本を指す記述に縮約する。 |
| 4 | アブダクション | 画像、画面生成prompt、実装の構成差は後続の仕様追加で説明できる。画像を現行仕様そのものと呼ばない。 |
| 5 | 垂直思考 | チャンネル変更の本質は旧データの混在防止。削除の完了条件を連携可否まで追跡する。 |
| 6 | 要素分解 | 設定は5区画の一括取得とメンバーの独立取得に分かれる。意図ある境界を保つ。 |
| 7 | MECE | 取込の受付と解析は別機能で、引渡し契約が曖昧。各機能の入口・成果・状態遷移を区別する。 |
| 8 | 2軸思考 | 「現機能/後続機能」×「実装/仕様」で確認する。チャンネル削除の実行は追加スライスで実装し、解析・テナント全削除・30日保持・収集は後続に残る。局所受入と製品全体を区別する。 |
| 9 | プロセス思考 | 実装は解除→削除予約→再連携可能、承認済み仕様は削除完了後の再連携。時系列を一致させる。 |
| 10 | メタ思考 | `docs/screens/README.md` はPNGをスナップショットとする一方、他文書は画面の正と呼ぶ。資料の役割を揃える。 |
| 11 | 抽象化思考 | 共通化の単位は表の見た目だけでなく、区画の権限とデータ寿命。独立したメンバー取得は維持する。 |
| 12 | ダブル・ループ思考 | 「画像への厳密一致」を目的にすると、後から承認されたメンバー管理を失う。現在の利用者の作業を基準にする。 |
| 13 | ブレインストーミング | 未使用UIの削除、静的ページの共通生成、取込規則の共有などを列挙し、重複を減らす効果と変更コストで採否を決める。 |
| 14 | 水平思考 | 初回のYouTube連携ではGoogle Cloud接続情報の登録が先に必要。未登録からの実際の操作順を確認する。 |
| 15 | 逆説思考 | 全データを1つのGETへ寄せると、その失敗でメンバー管理まで止まる。独立APIを残し、説明だけ正確にする。 |
| 16 | 類推思考 | Reactと静的HTMLに同じフッター文言を手入力するとドリフトする。共通の出力元または自動照合を検討する。 |
| 17 | if思考 | 案内リンクから取得したCSVが解析不能なら、受付の成功が利用者の目的を満たさない。解析機能の入力契約を固定する。 |
| 18 | 素人思考 | 初見では連携・削除が既に使えるのにプライバシー文面は将来形。利用者向け説明を現状に合わせる。 |
| 19 | システム思考 | 設定はOAuth、日次収集、取込解析、削除をつなぐ。画面単体の合格だけで完結としない。 |
| 20 | 因果関係分析 | 解除でチャンネル行を消すと即再連携でき、テナント単位の旧CSV履歴が新チャンネルに見える。連携再開を削除完了に結び付ける。 |
| 21 | 因果ループ | 仕様の複写→部分更新→食い違い→説明の追加という循環がある。正本と派生文書の責務を明示する。 |
| 22 | トレードオン思考 | 設定の一括取得は通信を減らすが、使用量取得の失敗が全画面へ波及し得る。主要操作と補助指標の失敗を分離する。 |
| 23 | プラスサム思考 | 未使用`ShellFrame`の除去は読みやすさと変更安全性の双方を上げる。実際の重複を優先する。 |
| 24 | 価値提案思考 | 利用者は「安全に分析できるか」を知りたい。処理待ちの取込や意味の違うクォータ表示を完了と誤認させない。 |
| 25 | 戦略的思考 | Google APIの枠はテナント固有クライアントのプロジェクト単位になった。全テナントのunitsを単一10,000unitsで割らない。 |
| 26 | why思考 | 誤った使用率←合計/単一上限←カウンタにテナント軸がない←共有クライアント時代の設計←qa-075の影響を横断反映していない。測定単位まで直す。 |
| 27 | 改善思考 | 旧データ混在、測定単位、機能間契約、未使用部品、資料の順で修正する。 |
| 28 | 仮説思考 | 「予約後すぐ再連携可」は旧データがチャンネル別に隔離される場合のみ成立する。現行`imports`はテナント単位なので仮説は棄却する。 |
| 29 | 論点思考 | 部品数より、画面・API・後続処理が同じライフサイクルと測定単位を語れることが本題。 |
| 30 | KJ法 | 発見をライフサイクル、測定、表現重複、検証の4群に整理し、依存のある修正から進める。 |

## 照合した主な根拠

- 画面資料: `docs/screens/05-settings.png`、`docs/screens/README.md`、`docs/screens/prompts/05-settings.prompt.txt`
- 実装: `web/pages/SettingsPage.tsx`、`src/usecases/{settings,youtube,imports,usage}.ts`、`src/repositories/settings-repository.ts`、`migrations/0003_settings_channel_link.sql`
- 契約: `features/feat-settings-channel-link.md`、`docs/feat-settings-channel-link/{requirements,architecture,runbook,acceptance}.md`、`features/feat-{csv-media-ingest,retention-ops}.md`

## 検証の範囲

30法レビューの改善は1回の統合・再検証で収束した（上限3回）。その後、チャンネル削除の実行まで追加した。設定機能の実装と後続機能への契約についての最終判定は次のとおり。

| 条件 | 判定 | 確認したこと |
|---|---|---|
| 矛盾なし | PASS | 画像の5カードと現行6区画を区別し、チャンネル変更は旧データ削除完了後、未計測の使用率は未取得に統一した。 |
| 漏れなし | PASS | 連携開始・OAuth応答・候補確定・DB保存の削除待ちゲート、画面の待機表示、専用Queueで旧CSV履歴と孤立R2原本を消してから解放する一周の結合テストを確認した。 |
| 整合性あり | PASS | メンバー以外の5区画を設定APIから取得し、メンバーは独立APIから取得する。未使用の`ShellFrame`を削除し、取込の形式・容量は`src/domain/import-rules.ts`を画面とサーバで共有する。 |
| 依存関係整合 | PASS | チャンネル削除は本機能のQueue/Cronで実行し、テナント全削除・30日保持は`feat-retention-ops`、解析は`feat-csv-media-ingest`へ渡す。CSV原本受付は`/api/imports`と`imports.import_id`に一本化し、後続の`csv_imports`を同じ受付IDへ紐付ける。 |

検証: `pnpm test` 23ファイル214件成功。`pnpm typecheck`、`pnpm lint`、`pnpm check:repo`、`pnpm build`、`git diff --check` 成功。E2Eは3サイズで85件成功、既存の意図的な2件をスキップ。ローカルWorkers runtimeでは`/login`・`/privacy`が200、未ログインの`/api/settings`が401。手動Cron起動は成功し、専用Queueが1通を処理した。

このPASSは設定機能とチャンネル削除の範囲に限る。CSVの実解析、日次収集、テナント全削除、30日保持、Googleプロジェクトごとのクォータ計測は後続に残る。本物のGoogle同意、Cloudflare Analytics実値、公開環境のQueue/Cronと一周は未検証。`done_at`は完了時点で旧世代が空だった証跡であり、極端に遅れて着地する原本は旧世代へ隔離して後続sweepで消す。
