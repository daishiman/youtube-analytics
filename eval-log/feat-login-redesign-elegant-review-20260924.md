# ログイン刷新のエレガントレビュー（2026-09-24）

対象: 今回のワークツリーの `feat-login-redesign`。思考リセット後、読み取り専用の3分析を並列実行した。画像の扱いは既存の利用者決定 `qa-063`・`qa-067` に従い、配置と文言を `docs/screens/01-login.png`、色・背景・ロゴを共通デザイン、Google ボタンを Light テーマの正本とする。

## 思考リセット

実装は React/Vite の画面、Hono の認証 API、D1 の保存、仕様・受入文書・E2E からなる。第一印象では、画像と既存のローカル証跡の撮影状態と寸法が異なり、画像比較の合格判定を再検証する必要があった。既存の作業ファイルは削除していない。

## 30種の適用記録

| # | 思考法 | 観測・判断 | 改善への接続 |
|---|---|---|---|
| 1 | 批判的思考 | `acceptance.md` の画像一致主張は、異なる撮影状態の画像に依存する。 | 比較状態と判定を修正する。 |
| 2 | 演繹思考 | 共通正本のテキストロゴ・ライトのみという規則と、自作ロゴ・dark CSS が相反する。 | ロゴとテーマを是正する。 |
| 3 | 帰納的思考 | `TrustFooter` の使用はログイン画面1か所で、規約ページは静的 HTML。 | 共用しているという記述を直す。 |
| 4 | アブダクション | 画像と証跡の寸法差はカード最大幅・余白・開発フォーム表示で説明できる。 | 同条件で再撮影し寸法を確認する。 |
| 5 | 垂直思考 | A1 E2E は DOM 順とボタン色を検査するが、視覚的な寸法差を検出しない。 | 視覚証跡の検収を分けて記録する。 |
| 6 | 要素分解 | config→scope、同意→記録、grant→連携状態に分解すると URL の未知エラー除去が欠ける。 | URL の扱いを修正する。 |
| 7 | MECE | A1〜A10 の対応表には本番デプロイ未実施と画像比較の不足がある。 | 未確認項目を合格に含めない。 |
| 8 | 2軸思考 | 利用者影響と修正量で、連携状態・古い config が高優先、文書重複が次点。 | 依存順に修正する。 |
| 9 | プロセス思考 | `pnpm check:repo` は6件で失敗し、PR の検査経路で実行される。 | 生成物の整合を回復する。 |
| 10 | メタ思考 | 画像の受入手法自体が DOM と3色だけで十分かを再評価した。 | 実際の画面状態を揃えて確認する。 |
| 11 | 抽象化思考 | ログインの本質は権限説明・同意・認証。自作ロゴと1用途の badge module は価値を増やさない。 | 不要な部品を減らす。 |
| 12 | ダブル・ループ思考 | 画像の全画素一致という前提は `qa-063`・`qa-067` の明示決定に反する。 | 正本の適用範囲を明記する。 |
| 13 | ブレインストーミング | 撮影状態合わせ、ロゴ除去、文書の正本整理、コンポーネント削減を候補化した。 | 重複を避ける最小の修正を採用する。 |
| 14 | 水平思考 | 新規画面だけでなく招待・エラー・読み込み・360px の状態を横断した。 | 状態遷移の回帰を確認する。 |
| 15 | 逆説思考 | 装飾を除いても権限・同意・信頼の情報順は成立する。 | 装飾の追加を修正対象にしない。 |
| 16 | 類推思考 | PNG と prompt を両方「正本」とすると版の異なる API schema と同じ曖昧さが生じる。 | 画像と仕様の権限を分ける。 |
| 17 | if思考 | SPA で invite query を切り替えると旧 config のままボタンが有効になり得る。 | config を invite に紐付けて失効させる。 |
| 18 | 素人思考 | 未チェック・開発フォーム付きの証跡では画像と同じ状態に見えない。 | 利用者が比較できる状態で撮影する。 |
| 19 | システム思考 | OAuth→D1→`/api/me`→バナー→後続収集の鎖では `linked` が利用可能な token を保証しない。 | 連携状態の不変条件を強める。 |
| 20 | 因果関係分析 | scope が完全でも初回 refresh token が無ければ継続取得できない。 | 保存済み token の有無を判定に含める。 |
| 21 | 因果ループ | 誤って `linked` になるとバナーが消え、再連携への回復経路が失われる。 | 不完全な連携に導線を出す。 |
| 22 | トレードオン思考 | 部分許可でもログインする価値は保ち、連携だけを未完了と示せる。 | 認証成否と連携成否を分ける。 |
| 23 | プラスサム思考 | `none` と `partial` の回復を同じ既存バナーで扱える。 | 新しい画面・API を増やさない。 |
| 24 | 価値提案思考 | 選択中が viewer の複数所属者では Google grant が捨てられ、説明した価値が届かない。 | 一意の owner テナントへ保存する。 |
| 25 | 戦略的思考 | `SCOPE_SETS` は表示と要求の共通定義として有効。 | 既存の正本を維持し、保存先だけ直す。 |
| 26 | why思考 | なぜ連携不能で linked か→scope だけで判定→なぜ token を含めないか→状態が許可と利用可能性を兼用→なぜ兼用か→後続収集との接続条件が未定義→なぜ未定義か→OAuth と収集を別々に設計→なぜ見逃したか→初回 token 不在のテストがない、の5段を追った。 | token を含む連携不変条件を定める。 |
| 27 | 改善思考 | 初回 token 不在、`none`、viewer 選択、invite 切替を改善点として抽出した。 | 既存部品を使って修正する。 |
| 28 | 仮説思考 | 「全 scope・token 無し」なら現行は linked と予測し、初回 token 無しのテスト欠落を確認した。 | 回帰テストを追加する。 |
| 29 | 論点思考 | 真の論点は表示 scope・実付与・対象テナント・利用可能な連携状態の一致。 | 4点を同時に検証する。 |
| 30 | KJ法 | 所見を表示と OAuth、grant とテナント、状態と token、仕様と証跡の4群に整理した。 | 独立群を並列修正し、依存する検証を最後に行う。 |

## 検証4条件

初回判定: 矛盾なし **FAIL**、漏れなし **FAIL**、整合性あり **FAIL**、依存関係整合 **FAIL**。

### 第1改善・再検証

- `LoginPage` の古い invite 設定と同意を切替時に破棄し、設定取得失敗の理由と未知エラー URL の除去を追加。自作ロゴと1用途の `ReadOnlyBadge` module を削除した。
- `linked` を「YouTube の2スコープ + 同じ利用者の保存済み refresh token」に限定。初回 token 無しは `partial`、既存の `none` も連携導線を表示。grant は一意の owner テナントへ保存し、複数 owner の場合は推測しない。
- ライトテーマに統一し、重複した色定義を削除。948×1659・同意済み・開発フォームなしの [比較画像](../evidence/feat-login-redesign/login-948-production-like.png) ではカードが x=162、y=248、w=624、h=847。元画像との差は位置・寸法が数 px。色・背景・ロゴ・Google Light ボタンは qa-063/qa-067 に基づく意図的な差。
- 文書の誤った画像比較・部品共有・seed 状態を訂正。`pnpm test` **124/124**、`pnpm e2e` **69/69**、`pnpm lint`、`pnpm typecheck`、`pnpm build:web`、`pnpm check:release`、`git diff --check` が通過。ローカルの `http://localhost:8791/api/health` は 200 を返す。

### 残る判定

ワークツリー全体として **incomplete**。`pnpm check:repo` は当初6件から2件へ減ったが、旧 resync gate の source/architecture digest は正式な C14 再分解前なので更新していない。gate 指定の既存3 feature と、今回のログイン feature の graph node 1件に文書との意味的な差が残る。C14 は対象 ID を限定する引数がなく、4件とも Beads binding なので、対象外ノードや外部 tracker を変えずに適用できることを確認するまで graph を直接編集しない。main push 後の migration/deploy と実 Google OAuth の本番受入も未実施。

| 条件 | 現在 | 根拠 |
|---|---|---|
| 矛盾なし | FAIL | graph の4 feature node が更新済み context/Markdown と一致しない |
| 漏れなし | FAIL | C14 再分解、実 Google と deploy の受入が未完了 |
| 整合性あり | FAIL | `pnpm check:repo` に digest 不一致が2件残る |
| 依存関係整合 | FAIL | graph source lineage と現行仕様の対応を正式に再投影できていない |
